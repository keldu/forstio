#pragma once

namespace gin {
namespace schema {
template<typename... T>
struct Struct {
};

template<typename... T>
struct Union {
};

template<typename... T>
struct Array {
};

struct String {};
}
}
